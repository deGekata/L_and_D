#include <stdlib.h>
#include <stdio.h>

int main() {
    // int a = 0;
    // int* a_ptr = &a;
    // printf("a = %d, *aptr = %d\n", a, *a_ptr);
    // *&*((&*a_ptr + 1)-1) = 10;
    // &*((int*)0);
    // printf("a = %d, *aptr = %d\n", a, *a_ptr);
    // // if ((&(&a)) == &a) {
    // //     printf("yes");
    // // } else {
    // //     printf("No");
    // // }
    int a = 2, d = 0;
    return 0; 
}