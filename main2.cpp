#include <stdio.h>

int main() {
    ({if (!op_ || op_->type != NodeType::CUSTOM || op_->data.custom != operator_) {true;} else {false;};})
    ({if (1 || 2 != 10) {true;} else {false;}});
    return 0;
}